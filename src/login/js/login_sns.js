import './../css/login_sns.css';
import { useContext } from 'react';
import { MyContext } from '../../App';

const Login_sns = () => {
    const {api} = useContext(MyContext);

    return(
        <div>
            zxvc
        </div>
    )
}

export default Login_sns;